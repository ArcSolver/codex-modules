# Astro / build
node_modules/
dist/
.astro/

# Generated docs content — the canonical source is the repo's README files.
# Regenerated by `npm run sync:docs` (runs automatically before build/dev).
src/content/docs/
